# Add project dependencies. Install with:
#   pip install -r requirements.txt
# Use a recent Python (3.11 or 3.12 recommended).
openai>=1.3.0
numpy>=1.24
streamlit>=1.28.0
python-dotenv>=0.21.0
pandas>=1.5.0
# Optional: PyTorch required for forecasting trainer/predictor
ultralytics>=8.0.0
# Install if you plan to train models: `pip install torch` (check https://pytorch.org/)
torch>=2.0.0

#command to start the python
python c:\Users\morse\AgisFlowAI\mock_stream.py


#command if using a video file
python c:\Users\morse\AgisFlowAI\mock_stream.py --video data/panic.mp4


#command to start with the device camera 
python c:\Users\morse\AgisFlowAI\mock_stream.py --video 0


#dashboard
python -m streamlit run c:\Users\morse\AgisFlowAI\crowdguard_dashboard.py