# Add project dependencies. Install with:
#   pip install -r requirements.txt
# Use a recent Python (3.11 or 3.12 recommended).
openai>=1.3.0
numpy>=1.24
streamlit>=1.28.0
python-dotenv>=0.21.0
pandas>=1.5.0
requests>=2.31.0
# Optional: PyTorch required for forecasting trainer/predictor
ultralytics>=8.0.0
# Install if you plan to train models: `pip install torch` (check https://pytorch.org/)
torch>=2.0.0

# =================================================================
# IMPORTANT: Project Command Reference
# The lines below are not packages; they are example commands.
# =================================================================
#
# 1. Install all dependencies from this file:
# pip install -r c:\Users\morse\AgisFlowAI\requirements.txt
#
# 2. Run the stream with a webcam or a device camera:
# python mock_stream.py --video 0
#
# 3. Run the stream with a local video file:
# python c:\Users\morse\AgisFlowAI\mock_stream.py --video data/sampel2.mp4
#
# 4. View the live dashboard:
# python -m streamlit run crowdguard_dashboard.py
#
# 5. Train the anomaly detection model:
# python src/autoencoder.py